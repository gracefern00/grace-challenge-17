const User = require('./User');
const Thought = require('./Thought');

module.exports = { User, Thought };
// This code imports the User and Thought models from their respective files and then exports them as an object. This allows other parts of the application to easily access both models by importing this module.